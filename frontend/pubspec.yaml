name: smart_campus_parking
description: A Smart Campus Parking System Frontend.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  dio: ^5.4.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.2
  shimmer: ^3.0.0
  google_fonts: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
